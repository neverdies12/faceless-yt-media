# faceless-yt-media

Public hosting for the faceless-yt cross-posting pipeline:

- `tiktok-callback.html` — TikTok OAuth redirect URI, displays the auth code for manual copy
- `media/` — videos published here become fetchable by Instagram's Graph API (which requires a public URL, not direct upload)

Served via GitHub Pages from the `main` branch.
